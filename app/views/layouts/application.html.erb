<!DOCTYPE html>
<html>
  <head>
    <title>LittleEstyShop7</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <style>
      body {
        font-family: Arial, sans-serif;
      }
      .header {
        font-family: Monaco;
        background-color: MediumSeaGreen;
        padding: 20px;
        text-align: center;
      }
      .subhead {
        font-family: Helvetica;
        border-left: 6px solid blue;
        background-color: lightgrey;
      }
      ul {
        list-style-type: none;
        margin: 0;
        padding: 0;
        overflow: hidden;
        background-color: #333;
      }
      li {
        float: left;
      }
      li a {
        display: block;
        color: white;
        text-align: center;
        padding: 14px 16px;
        text-decoration: none;
      }
      /* li a:hover:not(.active) {
        background-color: #111;
      } */
      .active {
        background-color: #04AA6D;
      }

      p.solid {
        border-style: solid;
        margin: auto;  
        padding: 16px;
        text-align: center;
      }

      .button {
        display: flex;
        justify-content: space-between;
        margin-right: 1200px;
      }

      /* USED FOR SPLITTING INDEX COLUMNS */
      .two-columns{
        display:flex;
      }

      .two-columns > * {
        width: 50%;
        padding: 1rem;
      }

      /* USED FOR TRIPLE SPLITTING INDEX COLUMNS */
      .three-columns{
        display:flex;
      }

      .three-columns > * {
        width: 33%;
        padding: 1rem;
      }

      /* USED FOR SHORTENING A PAGE WITH MULTIPLE COLUMNS */
      /* Create two equal columns that floats next to each other */
      .column {
        float: left;
        width: 50%;
        padding: 10px;
        height: 300px; /* Should be removed. Only for demonstration */
      }

      .button-container {
        white-space: nowrap; 
        /* Prevent line breaks */
      }

      .button-container form {
        display: inline-block; 
        /* Display the forms horizontally */
        margin-right: 4px; 
        /* Add some spacing between the buttons */
      }

      /* ADMIN FORMATTING CSS */
      .admin_header {
        font-family: Arial;
        font-size:30px;
        font-weight: bold;
        background-color: #bcb0eb;
        padding: 20px;
        text-align: center;
      }
      .admin_sub_header {
        font-family: Arial;
        font-size:21px;
        border-top: 2px solid #622583;
        font-weight: bold;
        background-color: #bcb0eb;
        padding: 20px;
        text-align: center;
      }
      .current_button {
        background-color:#bcb0eb;
        border-radius:22px;
        display:inline-block;
        font-family:Arial;
        font-size:12px;
        padding:16px 19px;
        text-decoration:none;
        float: right;
      }
      .nav_button {
        border-radius:22px;
        display:inline-block;
        cursor:pointer;
        font-family:Arial;
        font-size:12px;
        padding:16px 19px;
        text-decoration:none;
        float: right;
      }
      .nav_button:hover {
        background-color:#d5b0eb;
      }
      .nav_button:active {
        position:relative;
        top:1px;
      }
      .admin_sub {
        font-family: Arial;
        background-color: #bcb0eb;
        border-top: 2px solid #622583;
        padding: 2px 200px;
        text-align: center;
        float: left;
      }

      .enlarge_list {
        font-size:20px;
        line-height: 1.5;
      }

    </style>
    <% flash.each do |type, message| %>
      <h3><p><%= message %></p></h3>
    <% end %>
    <%= yield %>
  </body>
</html>
