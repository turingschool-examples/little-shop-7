<h2 class="admin-invoice-title">All Invoices</h2>

<ul>
  <% @invoices.each do |invoice| %>
    <li>
      <%= link_to "Invoice ##{invoice.id}", admin_invoice_path(invoice) %>
    </li>
  <% end %>
</ul>